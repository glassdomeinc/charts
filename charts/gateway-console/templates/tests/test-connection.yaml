apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gateway-console.fullname" . }}-test-connection"
  labels:
    {{- include "gateway-console.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gateway-console.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
