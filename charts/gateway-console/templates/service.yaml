apiVersion: v1
kind: Service
metadata:
  name: {{ include "gateway-console.fullname" . }}
  labels:
    {{- include "gateway-console.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gateway-console.selectorLabels" . | nindent 4 }}
